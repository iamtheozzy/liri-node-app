require("dotenv").config();


// requires files where they keys are stored
var keys = requre("./keys.js");
var Twitter = require('twitter');

// storing variables for node selectors
var node = process.argv;
var commands = node[2];

if (commands === "my-tweets"){


}
